uuid: a5006d19-2f01-48ec-a1d7-06e243e1b932
langcode: en
status: true
dependencies:
  config:
    - node.type.landing_page
  module:
    - content_moderation
id: landing_page_approval
label: 'Landing Page Approval'
type: content_moderation
type_settings:
  states:
    approved:
      published: false
      default_revision: false
      label: Approved
      weight: 0
    draft:
      label: Draft
      published: false
      default_revision: false
      weight: -2
    pending:
      published: false
      default_revision: false
      label: Pending
      weight: -1
    published:
      label: Published
      published: true
      default_revision: true
      weight: 1
  transitions:
    approved:
      label: Approved
      from:
        - pending
      to: approved
      weight: 0
    create_new_draft:
      label: Draft
      to: draft
      weight: -3
      from:
        - draft
    pending_approval:
      label: 'Pending Approval'
      from:
        - draft
      to: pending
      weight: -2
    publish:
      label: Publish
      to: published
      weight: 1
      from:
        - approved
        - draft
        - published
    rejected:
      label: Rejected
      from:
        - pending
      to: draft
      weight: -1
  entity_types:
    node:
      - landing_page
  default_moderation_state: draft
